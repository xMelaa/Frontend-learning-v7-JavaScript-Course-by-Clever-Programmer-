// let weather = prompt("How is the weather?");

// if(weather == 'rainy'){
//   console.log('Grab your umbrella');
// } else {
//   console.log('Wear your sunglasses');
// }

// ------------------LETTER FREQUENCY------------------------
/*const letterFrequency = (phrase) => {
  let freq = {};
  
  for (const letter of phrase){
    if(letter in freq) //if letter exists in freq
      freq[letter]++; //increment by +1
    else freq[letter] = 1;  //otherwise set value to 1
  }  
  return freq;
}

console.log(letterFrequency("haha hihi"));

const wordFrequency = (phrase) => {
//  let freq = {};

  wordArray = phrase.split(" " || ", "); // make new array with every wordfrom first phrase split in space
  for (const word of wordArray){
    if(word in freq) //if lword exists in freq
      freq[word]++; //increment by +1
    else freq[word] = 1;  //otherwise set value to 1 
  }  
  return freq;

  return letterFrequency(wordArray);
} 

console.log(wordFrequency("haha hihi haha hehe, give it")); */


